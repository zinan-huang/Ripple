name: Create Release

on:
  push:
    branches:
      - 'main'
      - 'master'
    paths:
      - 'lean-toolchain'

jobs:
  lean-release-tag:
    name: Add Lean release tag
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
    - name: lean-release-tag action
      uses: leanprover-community/lean-release-tag@v1
      with:
        # Lean-version tags record toolchain compatibility. Curated Ripple
        # releases use a separate rYYYY.MM[.N] namespace.
        do-release: false
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
